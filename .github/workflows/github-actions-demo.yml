name: hello-world
on: push
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - run: make say-hello
      - run: echo "🐧 This job is now running on a ${{ runner.os }} server hosted by GitHub!"
